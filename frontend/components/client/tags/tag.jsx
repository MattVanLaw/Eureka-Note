//removeTagging
//delteTag
//assume it will be passed tags object from client
import React from 'react';
import MenuContainer from './../menu/menu_container';

class TagsIndex extends React.Component {
  constructor(props) {
    super(props);
  }

  render () {
    return(
      <MenuContainer />

    );
  }
}

export default TagsIndex;
